CUDACachingAllocator，是一个对外接口组件。功能是将接口的调用信息转发给对应的DeviceCachingAllocator。在多卡环境下，CUDACachingAllocator管理着每个device（DeviceCachingAllocator）。存有一个字典，能够将指针映射到对应的DeviceCachingAllocator，从而加速查找指针的归属。

MemPool是CUDACachingAllocator的真正管理对象。MemPool类是用shared_ptr管理的，这意味着在失去所有引用后，其会自动析构。CUDACachingAllocator存在一个默认的MemPool，其仅会随着CUDACachingAllocator的析构来析构。CUDACachingAllocator能够进行MemPool的切换。MemPool创建时，能够支持传入自定义的customMalloc和customFree函数，用来在创建DeviceCachingAllocator时进行替换。如果没有传入，则使用默认的cudaMalloc和cudaFree。

DeviceCachingAllocator管理BlockPool，BlockPool管理Block。

分配tensor流程：
1. 传入size，device，stream，选用当前MemPool内的DeviceCachingAllocator，调用DeviceCachingAllocator.malloc进行分配，并记录分配指针
2. DeviceCachingAllocator.malloc接收stream和size。先对size按512 Bytes进行圆整，然后处理cudaEvent以进行Block的回收。回收后，获取BlockPool（默认分为large_pool和small_pool，按照size进行划分，<=1MB的为small pool，其余为large pool）。然后尝试从BlockPool里面找到可用的block进行返回，如果没有可用的，则使用cudaMalloc进行分配（如果使用customMalloc，则使用customMalloc进行分配）。cudaMalloc返回的是一个segment，从segment内分割一个block，然后将剩余的segment放入池内回收。

释放tensor流程：
1. 传入一个指针，从CUDACachingAllocator的字典内找到指针对应的DeviceCachingAllocator，然后调用DeviceCachingAllocator.free来释放这个指针，
2. DeviceCachingAllocator.free会插入一个事件来lazy free